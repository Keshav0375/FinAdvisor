# Local Preferences

Skip beginner explanations — assume senior-level context.
Show real-world patterns, not toy examples.
Ask before guessing on ambiguous specs.
Always run /check before claiming a task is done.
Use concise commit messages (feat:, fix:, refactor:, test:, docs:, chore:).
Prefer showing code over explaining theory.

## Environment

- Python 3.12 via system/pyenv
- Node.js 20 LTS
- Docker Desktop on Windows
- API keys in .env (ANTHROPIC_API_KEY, VOYAGE_API_KEY, OPENAI_API_KEY, GOOGLE_API_KEY)
- PostgreSQL via Docker (pgvector/pgvector:pg16)
- Platform: Windows 10, PowerShell primary

## Session Start Ritual

1. Read ARCHITECTURE.md and TODO.md
2. Run /progress to show where we are
3. Suggest next task
4. Get approval before implementing
